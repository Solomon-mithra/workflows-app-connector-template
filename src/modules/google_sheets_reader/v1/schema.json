{
  "metadata": {
    "workflows_module_schema_version": "1.0.0"
  },
  "fields": [
    {
      "type": "string",
      "id": "sheet_id",
      "label": "Google Sheets ID",
      "description": "Enter the Google Sheets ID (the long string from the URL)",
      "default": ""
    },
    {
      "type": "object",
      "id": "sheet_name",
      "label": "Sheet Name",
      "description": "Select the sheet/tab to read (populated after entering Sheet ID)",
      "ui_options": { "ui_widget": "SelectWidget" },
      "content": {
        "type": ["managed"],
        "content_objects": [ { "id": "sheet_names" } ]
      },
      "default": ""
    }
  ]
}
